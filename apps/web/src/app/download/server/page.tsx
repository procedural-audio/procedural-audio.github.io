export default function ProcedureServer() {
  return (
    <main className="mx-auto flex max-w-3xl flex-col gap-6 px-6 py-12">
      <header className="space-y-2">
        <p className="text-sm uppercase tracking-wide text-muted-foreground">Procedure Server</p>
        <h1 className="text-3xl font-semibold tracking-tight">Scalable batch rendering for assets</h1>
      </header>
      <p className="text-muted-foreground">
        Render stems, one-shots, and longer assets from Procedure patches using scalable
        infrastructure. Automate offline rendering jobs, schedule updates, and manage outputs for
        pipelines where reproducibility and consistency matter.
      </p>
    </main>
  )
}

