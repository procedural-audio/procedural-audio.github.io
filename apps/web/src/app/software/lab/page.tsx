export default function ProcedureLab() {
  return (
    <main className="mx-auto flex max-w-3xl flex-col gap-6 px-6 py-12">
      <header className="space-y-2">
        <p className="text-sm uppercase tracking-wide text-muted-foreground">Procedure Lab</p>
        <h1 className="text-3xl font-semibold tracking-tight">Visual patching for generative sound</h1>
      </header>
      <p className="text-muted-foreground">
        Lab provides a node-based canvas for sketching modular DSP graphs. Capture ideas quickly,
        audition variations in real time, and document signal flow before exporting patches for
        downstream tools.
      </p>
    </main>
  )
}
